class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        n={}
        found = False
        for i in nums:
            if i not in n:
                n[i]=1
            else:
                found = True
                break
        return found